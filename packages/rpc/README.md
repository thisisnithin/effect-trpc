# @repo/rpc

RPC contract package for the Effect-based tRPC server.

This package contains:
- RPC route definitions (Todo and Project)
- Shared schemas (Project, Todo)
- Error types (base and domain errors)
- Router type exports

## Usage

```typescript
import { appRouter, type AppRouter } from '@repo/rpc';
```

